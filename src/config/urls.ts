const baseURL = 'https://test.teaching-me.org/categories/v1/open'

const urls = {
    categories: `${baseURL}/categories`,
    teachers: `${baseURL}/search`,
    averagePrice: `${baseURL}/average-price`,
}

export {urls}